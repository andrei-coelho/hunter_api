# %YAML 1.2
---
  
production_url:  http://localhost/hunter_api/
development_url: 
production:      false

# banco de dados
database:

  default:
    driver: mysql
    host: localhost
    dbname: hunter2
    user: root
    pass:
    charset: utf8
    port: